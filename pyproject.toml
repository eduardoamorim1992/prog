[project]
name = "prog"
version = "0.1.0"
description = "Painel Flask para ordens de servico"
requires-python = ">=3.10"
dependencies = [
  "flask>=3.0.0",
  "pandas>=2.0.0",
  "openpyxl>=3.1.0",
]

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
